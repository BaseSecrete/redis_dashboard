<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Redis Dashboard</title>
    <link rel="stylesheet" href="<%= url("application.css") %>">
  </head>

  <body>
    <header>
      <h1 class="logo">
        <a href="<%= url("/") %>">Redis Dashboard</a>
      </h1>
      <% if params[:server] %>
        <nav aria-label="site">
          <a class="page-menu-item <%= 'active' if active_page?("#{params[:server]}") %>" href="<%= url("/#{params[:server]}") %>">Info</a>
          <a class="page-menu-item <%= 'active' if active_page?("#{params[:server]}/config") %>" href="<%= url("/#{params[:server]}/config") %>">Config</a>
          <a class="page-menu-item <%= 'active' if active_page?("#{params[:server]}/clients") %>" href="<%= url("/#{params[:server]}/clients") %>">Clients</a>
          <a class="page-menu-item <%= 'active' if active_page?("#{params[:server]}/stats") %>" href="<%= url("/#{params[:server]}/stats") %>">Stats</a>
          <a class="page-menu-item <%= 'active' if active_page?("#{params[:server]}/slowlog") %>" href="<%= url("/#{params[:server]}/slowlog") %>">Slowlog</a>
          <a class="page-menu-item <%= 'active' if active_page?("#{params[:server]}/memory") %>" href="<%= url("/#{params[:server]}/memory") %>">Memory</a>
        </nav>
      <% end %>  
    </header>
    <main>
      <%== yield %>
    </main>
    <footer>
      <p>Made by <a href="https://basesecrete.com/en/" target="_blank" rel="noopener">Base Secrète</a>.  MIT License. <a href="https://github.com/BaseSecrete/redis_dashboard" target="_blank" rel="noopener nofollower">Fork on GitHub</a>.<br>
        <b>Rails developer?</b> Check out <a href="https://rorvswild.com" target="_blank" rel="noopener">RoRvsWild</a>, our Ruby on Rails application monitoring tool.
      </p>
    </footer>
  </body>
</html>
