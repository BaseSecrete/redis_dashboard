<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Redis Dashboard</title>
    <meta name=viewport content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="application.css">
  </head>

  <body>
    <header>
      <h1 class="page-title">
        <a href="<%= url("/") %>">Redis Dashboard</a>
        <% if params[:id] %>
          <span><%= page_title %></span>
        <% end %>
      </h1>
      <% if params[:id] %>
        <nav aria-label="site">
          <a class="page-menu-item <%= 'active' if active_page?('info') %>" href="<%= url("/info?id=#{redis_id}") %>">Info</a>
          <a class="page-menu-item <%= 'active' if active_page?('config') %>" href="<%= url("/config?id=#{redis_id}") %>">Config</a>
          <a class="page-menu-item <%= 'active' if active_page?('clients') %>" href="<%= url("/clients?id=#{redis_id}") %>">Clients</a>
          <a class="page-menu-item <%= 'active' if active_page?('slowlog') %>" href="<%= url("/slowlog?id=#{redis_id}") %>">Slowlog</a>
        </nav>
      <% end %>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer>
        <b>Rails developer?</b> Check out <a href="https://rorvswild.com" target="_blank" rel="noopener">RoRvsWild</a>, our Ruby on Rails application monitoring tool.<br>
        Made by <a href="https://www.basesecrete.com/" target="_blank" rel="noopener">Base Secrète</a>.  MIT License. <a href="https://github.com/BaseSecrete/redis_dashboard" target="_blank" rel="noopener nofollower">Fork on GitHub</a>.
    </footer>
  </body>
</html>
